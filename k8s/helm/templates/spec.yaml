apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "usermgt.name" . }}-spec
  labels:
    {{- include "usermgt.labels" . | nindent 4 }}
data:
  current: "{{ .Chart.AppVersion }}"
  "{{ .Chart.AppVersion }}": |
    {{- .Files.Get "dogu.json" | nindent 4 }}
