apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "usermgt.name" . }}-config
  labels:
    {{- include "usermgt.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
  sensitive-config.yaml: |
    {}
